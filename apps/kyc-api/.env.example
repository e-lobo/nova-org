# Application Environment
# Possible values: development | production | test
NODE_ENV=development

# Server Configuration
# Port number for the server to listen on
PORT=3000

# API Configuration
# Full URL for API endpoints
API_URL=http://localhost:8080

# Database Configuration
# PostgreSQL connection string
# Format: postgresql://[user]:[password]@[host]:[port]/[db_name]
DATABASE_URL=postgresql://user:password@localhost:5432/dbname_dev

# Logging Configuration
# Possible values: debug | info | warn | error
LOG_LEVEL=debug

# CORS Configuration
# Allowed origins for CORS
CORS_ORIGIN=http://localhost:3000

# Rate Limit Configuration
# Maximum number of requests allowed
RATE_LIMIT_MAX_REQUESTS=100

# JWT Configuration
# Secret key for JWT
JWT_SECRET=secret

# Note: This is an example file. Create a copy named .env in the env/ directory
# and update the values according to your environment.
# 
# For different environments:
# - Development: copy to env/.env.development
# - Production:  copy to env/.env.production
# - Test:        copy to env/.env.test
#
# Make sure to set secure values in production and never commit actual .env files